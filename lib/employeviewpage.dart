import 'package:flutter/material.dart';

class EmployeeViewpage extends StatelessWidget {
  const EmployeeViewpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}

